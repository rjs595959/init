let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_cf9‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_cf9‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_cf9‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_cf9‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_cf9‍.w("./api",[["default",["api"],function(v){api=v}]]);_cf9‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

// DB 연결
mongoose
.connect(MONGO_URI, {
    useUnifiedTopology: true,
    useNewUrlParser: true,
    useFindAndModify: false,
    useCreateIndex: true,
})
.then(() => {
    console.log('Connected To MongoDB');
})
.catch(e => {
    _cf9‍.g.console.log(e);
})

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());
app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    console.log(`Listening to port ${port}`);
});